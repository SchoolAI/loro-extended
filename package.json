{
  "name": "loro-extended",
  "version": "1.0.0",
  "private": true,
  "description": "Extended capabilities for the Loro CRDT library",
  "keywords": [],
  "author": "Duane Johnson",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "build:all": "pnpm -r build && pnpm -r build:all",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "test:all": "pnpm -r test && pnpm -r test:all",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.7",
    "@changesets/cli": "^2.29.7",
    "@halecraft/verify": "^1.0.0"
  }
}
