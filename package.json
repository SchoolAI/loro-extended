{
  "name": "loro-extended",
  "version": "1.0.0",
  "private": true,
  "description": "Extended capabilities for the Loro CRDT library",
  "keywords": [],
  "author": "Duane Johnson",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "build:all": "pnpm -r build && pnpm -r build:all",
    "verify": "pnpm -r --parallel verify"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.7",
    "@changesets/cli": "^2.29.7",
    "@halecraft/verify": "^1.0.0"
  },
  "pnpm": {
    "overrides": {
      "react": "19.1.0",
      "react-dom": "19.1.0"
    }
  }
}
